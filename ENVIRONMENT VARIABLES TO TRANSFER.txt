These are all the variables to transfer BUT we need to be careful to catch which ones need to be changed and updated since the Neon database is also transferred to a new Neon account and of course we need to be careful to not lose data when transferring the whole thing so how does cloning manual or whatever work?

X_BEARER_TOKEN
AAAAAAAAAAAAAAAAAAAAAOKx4gEAAAAAyUTLAF40jJtkdWGOfO5lYixq9Nk%3DAzGBJSHVASSynRKJ9sckRD9ozSeBGzM2jk250mVznL5hHdhzpf

X_USERNAME
HerculisCoin

CRON_SECRET
xauh_cron_secret_2024_secure_key_do_not_share


############# Supposedly Neon stuff but I don't know ###########################
###############################################################################

# Recommended for most uses
DATABASE_URL=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require

# For uses requiring a connection without pgbouncer
DATABASE_URL_UNPOOLED=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt.eu-west-2.aws.neon.tech/neondb?sslmode=require

# Parameters for constructing your own connection string
PGHOST=ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech
PGHOST_UNPOOLED=ep-soft-hat-abe0mhqt.eu-west-2.aws.neon.tech
PGUSER=neondb_owner
PGDATABASE=neondb
PGPASSWORD=npg_SdP0Mmo1RNLs

# Parameters for Vercel Postgres Templates
POSTGRES_URL=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt.eu-west-2.aws.neon.tech/neondb?sslmode=require
POSTGRES_USER=neondb_owner
POSTGRES_HOST=ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech
POSTGRES_PASSWORD=npg_SdP0Mmo1RNLs
POSTGRES_DATABASE=neondb
POSTGRES_URL_NO_SSL=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech/neondb
POSTGRES_PRISMA_URL=postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech/neondb?connect_timeout=15&sslmode=require

# Neon Auth environment variables for Next.js
NEXT_PUBLIC_STACK_PROJECT_ID=****************************
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=****************************************
STACK_SECRET_SERVER_KEY=***********************

psql "postgresql://neondb_owner:npg_SdP0Mmo1RNLs@ep-soft-hat-abe0mhqt-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require"

import { Pool } from 'pg';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: {
    rejectUnauthorized: false,
  },
});

async function getData() {
  const client = await pool.connect();
  try {
    const { rows } = await client.query('SELECT * FROM posts');
    return rows;
  } finally {
    client.release();
  }
}

export default async function Page() {
  const data = await getData();
  return (
    <div>
      {data.map((post, index) => (
        <div key={index}>
          <h2>{post.title}</h2>
          <p>{post.content}</p>
        </div>
      ))}
    </div>
  );
}

import postgres from 'postgres';

const sql = postgres(process.env.DATABASE_URL,  { ssl: 'verify-full' });


// prisma/schema.prisma
datasource db {
  provider  = "postgresql"
  url  	    = env("DATABASE_URL")
}

################################################################################

